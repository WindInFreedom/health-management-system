# 替换本地内容：扩展依赖以支持MySQL、Channels、S3和预测模型
# Security: Updated to Django 4.2.26 to fix multiple vulnerabilities:
# - CVE: SQL injection in column aliases
# - CVE: SQL injection in HasKey(lhs, rhs) on Oracle
# - CVE: Denial-of-service in HttpResponseRedirect on Windows
# - CVE: Denial-of-service in intcomma template filter
# - CVE: SQL injection via _connector keyword argument
Django==4.2.26
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.1
django-filter==23.3

# Database
mysqlclient==2.2.0

# WebSocket support for real-time admin dashboard
channels==4.0.0
channels-redis==4.1.0

# Object storage for avatars
boto3==1.34.10

# Numerical and statistical libraries
numpy==1.24.3
scipy==1.11.4
pandas==2.0.3

# Time series forecasting
statsmodels==0.14.1
scikit-learn==1.3.2

# Password hashing
argon2-cffi==23.1.0

# Testing
pytest==7.4.3
pytest-django==4.7.0
pytest-cov==4.1.0
pytest-asyncio==0.21.1
